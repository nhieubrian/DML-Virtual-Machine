apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: xgl-cache-vol
  namespace: suncave
spec:
  storageClassName: rook-ceph-block
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Ti
